#!/bin/sh
set -e

if [ "$1" = "purge" ]; then
	cat <<'MSG'

status-bar removed. Two things are left behind on purpose, per user:

  ~/.config/claude-status/config.json   your settings
  ~/.claude/settings.json               hook entries pointing at
                                        /usr/lib/claude-status/hooks/emit.sh

Delete them by hand if you want them gone.

MSG
fi

exit 0
